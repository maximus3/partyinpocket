# Вечеринка в кармане

Android-приложение с коллекцией игр для вечеринок. Играйте с друзьями в классические и новые игры прямо на вашем смартфоне!

## 🎮 Доступные игры

### Шляпа
Классическая игра для компании из 3 раундов:

1. **Объяснения** - объясни слово словами, не используя однокоренные
2. **Пантомима** - покажи слово жестами, без слов и звуков
3. **Ассоциация** - скажи одно слово-ассоциацию

**Особенности:**
- От 2 до 6 команд
- Настраиваемое количество слов (10-100)
- Регулируемое время хода (30-120 секунд)
- 11 готовых наборов слов (общие, животные, фильмы, знаменитости, еда, профессии, страны, действия)
- **Генерация слов с помощью ИИ** - создавайте уникальные наборы на любую тему
- Система пропусков с штрафными очками
- Подсчет очков по раундам

## ✨ Ключевые возможности

### Генерация слов с ИИ
Создавайте уникальные наборы слов на любую тему:
- Введите тему (например, "IT-технологии", "Путешествия", "Хобби")
- ИИ сгенерирует подходящие слова для игры
- Автоматические повторные попытки (до 3 раз)
- Сохранение сгенерированных наборов для повторного использования

Для использования этой функции:
1. Откройте настройки через главный экран
2. Получите бесплатный API-ключ на [OpenRouter](https://openrouter.ai/settings/keys)
3. Вставьте ключ в настройки
4. Готово! Теперь можно генерировать слова при настройке игры

### Настройки игры
- **Команды**: выбор количества команд, редактирование названий и цветов
- **Слова**: выбор наборов слов или генерация своих
- **Время**: настройка длительности хода
- **Пропуски**: включение/выключение пропусков и штрафных очков

## 🚀 Установка и запуск

### Требования
- Android Studio (с JDK)
- Android SDK
- Устройство или эмулятор с Android 7.0+
- make (для удобных команд)

### Быстрые команды (Makefile)

```bash
# Показать все доступные команды
make help

# Собрать release и debug APK
make build

# Установить на устройство
make install

# Увеличить версию
make bump-version

# Сгенерировать иконку
make icon

# Скопировать иконку в приложение
make copy-icon
```

### Сборка вручную
```bash
# Установка переменной окружения
export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"

# Сборка APK
./gradlew :app:assembleDebug

# Установка на устройство
~/Library/Android/sdk/platform-tools/adb install -r app/build/outputs/apk/debug/app-debug.apk
```

## 🧪 Тестирование

В проекте есть unit-тесты на бизнес-логику игры "Шляпа":
- **State machine игры** — переходы между раундами, переключение команд, таймер, сохранение времени между раундами
- **Доменные модели** — `Team` (счёт, цвета), `HatGameState` (вычисляемые свойства), `HatRound` (порядок раундов)
- **Управление наборами слов** — поиск, добавление и удаление сгенерированных пакетов в `PresetWordPacks`

Тесты гоняются на JVM без эмулятора (~5 секунд).

### Запуск тестов

```bash
# Быстрая команда через Makefile
make test

# Или напрямую через Gradle
./gradlew testDebugUnitTest

# С подробным выводом
./gradlew testDebugUnitTest --info
```

Визуальный отчёт после запуска: `app/build/reports/tests/testDebugUnitTest/index.html`

Тесты запускаются автоматически в GitHub Actions перед сборкой APK — упавший тест блокирует сборку.

### Добавление тестов для новой игры

При добавлении новой игры (Алиас, Крокодил, Шпион и т.д.) пишите тесты параллельно с реализацией:
1. Тесты на доменные модели → `app/src/test/java/com/m3games/partyinpocket/domain/model/<игра>/`
2. Тесты на ViewModel игры → `app/src/test/java/com/m3games/partyinpocket/presentation/screens/<игра>/`
3. Перед коммитом запустите `make test` — убедитесь, что не сломался функционал старых игр.

Подробности в [CLAUDE.md](CLAUDE.md#testing).

## 🔐 Подпись релиз-сборок

Release APK подписывается production keystore из `keystores/partyinpocket-release.keystore`. Это тот же ключ, которым подписаны все версии в RuStore начиная с v0.0.4 — без него обновление поверх установленного приложения не пройдёт (пользователю придётся удалить и поставить заново).

**Локально:**
1. В корне проекта должен лежать файл `keystore.properties` (он в `.gitignore`, не уйдёт в git).
2. Без него `make build-release` упадёт с понятным сообщением — это специально, чтобы не подписать APK debug-ключом и не сломать обновления.
3. Формат файла, пароли и инструкции по восстановлению: [keystores/README.md](keystores/README.md).

**В GitHub Actions:**
Сборка release/публикация требуют двух секретов в Settings → Secrets and variables → Actions:
- `KEYSTORE_BASE64` — keystore в base64 (`base64 -i keystores/partyinpocket-release.keystore | pbcopy`)
- `KEYSTORE_PROPERTIES` — содержимое `keystore.properties` (`cat keystore.properties | pbcopy`)

Без этих секретов workflows `build.yml` и `release.yml` упадут на шаге `Setup keystore`.

**Резервная копия — обязательна.** Если потеряете keystore, выпускать обновления станет невозможно (придётся публиковать как новое приложение с другим packageId). См. раздел "Создание резервной копии" в [keystores/README.md](keystores/README.md).

## 🛠 Технологии

- **Язык**: Kotlin
- **UI**: Jetpack Compose + Material Design 3
- **Архитектура**: MVVM с ручным внедрением зависимостей
- **Навигация**: Navigation Compose
- **Асинхронность**: Coroutines + Flow
- **HTTP-клиент**: Ktor
- **Сериализация**: kotlinx.serialization
- **Хранение данных**: SharedPreferences

## 📱 Скриншоты

_Скриншоты будут добавлены позже_

## 🎯 Планы развития

### Новые игры
- **Элиас** - объясняй слова за время
- **Шпион** - найди шпиона среди игроков
- **Крокодил** - показывай слова жестами
- **Данетки** - разгадывай загадки с вопросами
- **Мафия** - классическая игра с ведущим
- И многое другое!

### Улучшения
- Сохранение истории игр
- Статистика по командам и игрокам
- Темная тема
- Звуковые эффекты и вибрация
- Многоязычность (английский, украинский)
- Облачное хранение наборов слов

## 🎨 Создание иконки приложения

### 1. Генерация иконки с помощью ИИ
```bash
cd icon-generator
uv run main.py generate -p Описание вашей иконки
```

### 2. Подготовка иконок для Android
```bash
# Конвертирует PNG в нужные размеры для всех плотностей экрана
uv run prepare_app_icons.py output/20260105T123501_generated_icon.png
```

### 3. Проверка
```bash
cd ..
./gradlew clean assembleRelease
~/Library/Android/sdk/platform-tools/adb install -r app/build/outputs/apk/release/app-release.apk
```

## 🤖 GitHub Actions

Репозиторий настроен с автоматической сборкой:

### На каждый коммит в main
- Собираются debug и release APK
- Сохраняются как артефакты (доступны 30 дней)
- Можно скачать во вкладке Actions → Build APK

### На каждый тег (v*)
- Создаётся GitHub Release
- Автоматически прикрепляются оба APK
- Имена файлов: `PartyInPocket-v0.0.X.apk` и `PartyInPocket-v0.0.X-debug.apk`

### Создание релиза

```bash
# 1. Обновите версию
make bump-version

# 2. Создайте changelog для версии
mkdir -p for_release/v0.0.2
nano for_release/v0.0.2/CHANGELOG.md
# Опишите изменения (используйте for_release/CHANGELOG_TEMPLATE.md как шаблон)

# 3. Закоммитьте изменения
git add app/build.gradle.kts for_release/v0.0.2/
git commit -m "Bump version to 0.0.2"

# 4. Создайте тег
git tag v0.0.2

# 5. Запушьте
git push origin main
git push origin v0.0.2

# GitHub Actions автоматически создаст релиз с APK файлами и описанием из changelog
```

## 📦 Публикация в RuStore

### Описания для магазина

**Краткое описание** (до 80 символов):
```
Игры для вечеринок: Шляпа с ИИ-генерацией слов. Играйте с друзьями!
```

**Полное описание**:
```
🎉 Вечеринка в кармане — коллекция игр для компании прямо на вашем смартфоне!

🎩 ИГРА "ШЛЯПА"
Классическая игра для весёлой компании из 3 увлекательных раундов:
• Объяснения — опиши слово, не используя однокоренные
• Пантомима — покажи жестами без слов
• Ассоциация — одно слово-подсказка

✨ ВОЗМОЖНОСТИ
• От 2 до 6 команд с настраиваемыми названиями и цветами
• 10-100 слов на игру
• Таймер от 30 до 120 секунд
• 11 готовых наборов: общие, животные, фильмы, знаменитости, еда, профессии, страны, действия и другие
• Система пропусков с штрафными очками
• Подробная таблица результатов по раундам

🤖 ГЕНЕРАЦИЯ СЛОВ С ИИ
Создавайте уникальные наборы слов на любую тему:
• IT-технологии, путешествия, хобби, спорт — что угодно!
• Нейросеть подберёт подходящие слова для вашей темы
• Сохраняйте созданные наборы для повторного использования
• Бесплатный API-ключ — никаких подписок!

🎯 ИДЕАЛЬНО ДЛЯ
• Вечеринок и дружеских встреч
• Семейных праздников
• Корпоративов
• Поездок и путешествий

📱 ПРОСТОЙ И УДОБНЫЙ
• Современный дизайн Material Design 3
• Интуитивный интерфейс
• Быстрая настройка игры
• Без рекламы и встроенных покупок

Соберите компанию, запустите приложение и начинайте веселиться! 🎊
```

### Автоматическая публикация (GitHub Actions)

Репозиторий настроен с автоматической публикацией в RuStore через GitHub Actions.

**Требования:**
1. Создайте секрет `RUSTORE_KEY` в Settings → Secrets → Actions
2. Соберите APK файл и создайте changelog

**Использование:**
1. Перейдите в Actions → Publish to RuStore
2. Нажмите "Run workflow"
3. Укажите параметры:
   - **Version**: версия для публикации (например, `0.0.4`)
   - **APK path**: путь к файлу (по умолчанию: `for_release/v{VERSION}/PartyInPocket-v{VERSION}.apk`)
   - **Submit for review**: отправить на модерацию сразу (по умолчанию: да, рекомендуется)
   - **Priority update**: приоритет обновления 0-5 (по умолчанию: 0)

**Что делает workflow:**
- Создаёт черновик версии в RuStore
- Загружает APK файл
- Читает changelog из `for_release/v{VERSION}/CHANGELOG.md`
- Отправляет на модерацию (черновик из API не виден в веб-интерфейсе RuStore)
- Выводит подробный отчёт со статусом модерации

### Сборка и публикация вручную

**Подробная инструкция:** См. [RELEASE.md](RELEASE.md)

### Быстрый старт

1. **Обновите версию в `app/build.gradle.kts`:**
   ```kotlin
   versionCode = 1
   versionName = "0.0.1"
   ```

2. **Соберите Release APK:**
   ```bash
   export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"
   ./gradlew clean assembleRelease
   ```

3. **Переименуйте и загрузите в RuStore:**
   ```bash
   cp app/build/outputs/apk/release/app-release.apk PartyInPocket-v0.0.1.apk
   # Загрузите в RuStore через консоль разработчика
   ```

## 🤝 Вклад в проект

Приветствуются любые предложения и улучшения! Создавайте issues и pull requests.

## 📄 Лицензия

Проект распространяется под лицензией [Apache License 2.0](LICENSE.md).

Вы можете свободно:
- ✅ Использовать в коммерческих проектах
- ✅ Модифицировать код
- ✅ Распространять
- ✅ Использовать патенты

При условии:
- 📝 Указать изменения в модифицированных файлах
- 📝 Сохранить копирайт и текст лицензии

## 👨‍💻 Автор

М3 Games

## 📞 Обратная связь

Нашли баг или есть идея? Создайте issue в репозитории!

---

**Играйте вместе, веселитесь больше! 🎉**
