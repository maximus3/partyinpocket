## 🎮 Улучшения игрового процесса

### Улучшения
- 🔀 **Перемешивание слов между ходами** - Теперь при переходе к следующей команде оставшиеся слова перемешиваются. Если последнее слово не угадали, следующая команда начнет с другого слова
- 📱 **Экран не гаснет во время игры** - Пока идёт таймер, экран устройства остаётся активным и не затемняется
- ⏱️ **Защита от случайных нажатий** - Кнопка "Следующая команда" блокируется на 3 секунды с показом обратного отсчета, чтобы случайно не пропустить результаты хода
- 🔙 **Диалог выхода из игры** - При нажатии кнопки "Назад" на экранах результатов появляется диалог подтверждения выхода с предупреждением о потере прогресса

### Технические изменения
- Добавлен `shuffled()` для `remainingWords` при смене команды в `HatViewModel`
- Добавлен `FLAG_KEEP_SCREEN_ON` для окна во время фазы `PLAYING` в `HatGameScreen`
- Добавлен 3-секундный таймер с `LaunchedEffect` для блокировки кнопок на экранах результатов
- Добавлен `BackHandler` с `AlertDialog` для подтверждения выхода на `HatTurnResultScreen` и `HatRoundResultScreen`
